<Window x:Class="Whisper_Messenger.Views.MediaPreview"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Whisper_Messenger.Views"
        mc:Ignorable="d"
        Title="MediaPreview" Height="550" Width="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Image Name="ShowMedia" Width="350" Height="400" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0" Margin="25,25,25,109" Stretch="Fill"></Image>
        <Button Name="SendButton" Content="Send" Width="150" Height="30" Grid.Column="0" Grid.Row="1" Margin="15" Command="{Binding SendFileClick}"></Button>
        <Button Name="CancelButton" Content="Cancel" Width="150" Height="30" Grid.Column="1" Grid.Row="1" Margin="15" Click="CancelButtonClick"></Button>
    </Grid>
</Window>
